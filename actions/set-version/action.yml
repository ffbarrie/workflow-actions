name: set-version
description: >
  Validates a human-entered version and writes it into the project's
  version file(s) — package.json for Node, pom.xml (and optionally
  application-release.yaml) for Java. On develop/main, the version must
  be strictly greater than the closest existing release tag reachable
  from HEAD (git describe --tags --abbrev=0, matching tag-prefix); other
  branches skip that comparison but still validate the version's format.
  Assumes checkout has already happened, with fetch-depth: 0 (tag history
  is required for the comparison) — and that language tooling (Node/npm
  or Java/Maven) is already set up, e.g. via setup-node or
  setup-java-maven earlier in the same job. Does not commit, tag, or push
  anything; that's left to later steps in the calling workflow. The
  application-release.yaml path writes app.version and app.build (a UTC
  build timestamp, yyyy-MM-ddTHH:mm:ssZ — matching the existing
  PowerShell release scripts' convention) and requires yq (present by
  default on GitHub-hosted ubuntu runners).

inputs:
  language:
    description: Which version file(s) to update — "node" or "java"
    required: true
  version:
    description: >
      Human-entered version to validate and apply, e.g. "1.4.0". A
      leading "v"/"V" is stripped automatically before validation.
    required: true
  working-directory:
    description: Directory containing package.json or pom.xml
    required: false
    default: "."
  tag-prefix:
    description: >
      Prefix used on existing release tags (e.g. "v" for v1.4.0) when
      finding the closest prior tag to compare against on develop/main.
    required: false
    default: "v"
  update-application-release-yaml:
    description: Java only — also write app.version into application-release.yaml
    required: false
    default: "false"
  application-release-yaml-path:
    description: Path to application-release.yaml, relative to working-directory. Only used when update-application-release-yaml is true.
    required: false
    default: "application-release.yaml"

outputs:
  success:
    description: "'true' if validation and file updates succeeded, 'false' otherwise"
    value: ${{ steps.validate.outputs.success }}
  error-message:
    description: Empty on success; a human-readable reason on failure
    value: ${{ steps.validate.outputs.error-message }}
  validated-version:
    description: The normalized version (no leading v) that was validated and applied. Empty on failure.
    value: ${{ steps.validate.outputs.validated-version }}

runs:
  using: composite
  steps:
    - name: Validate version
      id: validate
      shell: bash
      env:
        LANGUAGE: ${{ inputs.language }}
        VERSION: ${{ inputs.version }}
        TAG_PREFIX: ${{ inputs.tag-prefix }}
        BRANCH: ${{ github.ref_name }}
      run: |
        fail() {
          echo "::error::set-version: $1" >&2
          echo "success=false" >> "$GITHUB_OUTPUT"
          echo "error-message=$1" >> "$GITHUB_OUTPUT"
          echo "validated-version=" >> "$GITHUB_OUTPUT"
          exit 1
        }

        version_gt() {
          # returns success (0) if $1 is strictly greater than $2, both X.Y.Z
          IFS='.' read -r a1 a2 a3 <<< "$1"
          IFS='.' read -r b1 b2 b3 <<< "$2"
          if (( 10#$a1 != 10#$b1 )); then (( 10#$a1 > 10#$b1 )); return; fi
          if (( 10#$a2 != 10#$b2 )); then (( 10#$a2 > 10#$b2 )); return; fi
          (( 10#$a3 > 10#$b3 ))
        }

        if [ "$LANGUAGE" != "node" ] && [ "$LANGUAGE" != "java" ]; then
          fail "language must be 'node' or 'java', got '$LANGUAGE'"
        fi

        version="${VERSION#v}"
        version="${version#V}"
        if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          fail "version '$VERSION' is not a valid X.Y.Z semantic version"
        fi

        if [ "$BRANCH" = "develop" ] || [ "$BRANCH" = "main" ]; then
          closest_tag=$(git describe --tags --abbrev=0 --match "${TAG_PREFIX}[0-9]*.[0-9]*.[0-9]*" 2>/dev/null || true)
          if [ -n "$closest_tag" ]; then
            closest_version="${closest_tag#"$TAG_PREFIX"}"
            if ! version_gt "$version" "$closest_version"; then
              fail "version $version is not greater than $closest_tag, the closest existing release tag on $BRANCH"
            fi
          fi
        fi

        echo "success=true" >> "$GITHUB_OUTPUT"
        echo "error-message=" >> "$GITHUB_OUTPUT"
        echo "validated-version=$version" >> "$GITHUB_OUTPUT"

    - name: Update package.json
      if: inputs.language == 'node'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        VALIDATED_VERSION: ${{ steps.validate.outputs.validated-version }}
      run: npm version "$VALIDATED_VERSION" --no-git-tag-version --allow-same-version

    - name: Update pom.xml
      if: inputs.language == 'java'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        VALIDATED_VERSION: ${{ steps.validate.outputs.validated-version }}
      run: mvn -B versions:set -DnewVersion="$VALIDATED_VERSION" -DgenerateBackupPoms=false

    - name: Update application-release.yaml
      if: inputs.language == 'java' && inputs.update-application-release-yaml == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        VALIDATED_VERSION: ${{ steps.validate.outputs.validated-version }}
        YAML_PATH: ${{ inputs.application-release-yaml-path }}
      run: |
        set -euo pipefail
        if [ ! -f "$YAML_PATH" ]; then
          echo "::error::set-version: application-release-yaml-path '$YAML_PATH' does not exist"
          exit 1
        fi
        export BUILD_UTC
        BUILD_UTC=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
        yq -i '.app.version = strenv(VALIDATED_VERSION) | .app.build = strenv(BUILD_UTC)' "$YAML_PATH"
